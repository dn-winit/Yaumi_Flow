"""Shared utilities used by every backend service.

Modules:
    db_pool        -- bounded ODBC connection pool + retry decorator
    settings_base  -- shared helpers for pydantic-settings classes
"""
