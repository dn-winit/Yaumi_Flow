# API
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Data
pandas>=2.0,<3.0

# Database (cursor-level retries via common.db_pool)
pyodbc>=5.0,<6.0

# Scheduler (incremental import cron + audit hook via common.scheduler_audit)
APScheduler>=3.10,<4.0

# Observability (structured logging + Prometheus metrics) via common.observability
structlog>=24.1,<26.0
prometheus-client>=0.20,<1.0
