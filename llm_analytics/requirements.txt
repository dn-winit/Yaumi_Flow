# API
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Data (formatter consumes pandas dicts from upstream services)
pandas>=2.0,<3.0

# LLM providers -- groq is the default; uncomment to add fallbacks.
# Each provider's client is loaded lazily, so adding the package without
# wiring it doesn't slow startup.
groq>=0.4.0,<1.0
# openai>=1.0,<2.0
# anthropic>=0.30.0,<1.0

# Prompt templates (loaded once at startup; refusal patterns validated via Pydantic)
pyyaml>=6.0,<7.0
