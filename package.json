{
  "name": "yaumi-flow-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Repo-root wrapper: only exists so a root-level npm install bootstraps Husky's git hooks. Real webapp config lives in webapp/package.json.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.0"
  }
}
