# Core
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0,<2.0

# Data + numerics (recommendation engine + calibration)
pandas>=2.0,<3.0
numpy>=1.24,<3.0

# Database (push to YaumiAIML via common.db_pool)
pyodbc>=5.0,<6.0

# Scheduler (post-cron calibration runs via APScheduler + scheduler_audit)
APScheduler>=3.10,<4.0
