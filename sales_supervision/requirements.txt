# API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.0

# Upstream HTTP (live actuals via data_import)
httpx>=0.27

# Database (save sessions to YaumiAIML)
pyodbc>=5.0

# Data shaping
pandas>=2.0
